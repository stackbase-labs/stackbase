global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: stackbase-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - host.docker.internal:4000
        labels:
          service: api
          environment: local
